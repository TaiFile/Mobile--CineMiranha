services:
  Cinema-db:
    image: 'postgres:latest'
    container_name: Cinema-db
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
      - POSTGRES_DB=cinema-db
    ports:
      - "5432:5432"